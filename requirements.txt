# Python dependencies for the ABC123 Restaurant RAG web app.
#
# Pinned to the EXACT versions verified working on the source machine
# (pip list output) so the Docker image reproduces the tested environment.
# If you upgrade any of these, re-test chat.py / embedding.py locally first.
fastapi==0.141.1
uvicorn[standard]==0.34.0
langchain-core==1.6.0
langchain-ollama==1.1.0
langchain-qdrant==1.1.0
openpyxl==3.1.5
pydantic==2.13.4
